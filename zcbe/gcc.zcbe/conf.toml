# Config file for a project
# Placed in $SRCROOT/zcbe/conf.toml
[package]
name = "gcc"
ver = "15.1.0"

[deps] # Dependencies
build = [
"gfortran-mingw-w64"
]
req = [
"gmp",
"mpc",
"mpfr",
"binutils-gdb",
"zlib",
"zstd",
]

[env]
CC = "${ZCHOST}-gcc"
