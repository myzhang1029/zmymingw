# Config file for a project
# Placed in $SRCROOT/zcbe/conf.toml
[package]
name = "gcc"
ver = "16.1.0"

[deps] # Dependencies
build = [
"gfortran-mingw-w64"
]
req = [
"gmp",
"libiconv",
"mpc",
"mpfr",
"binutils-gdb",
"zlib",
"zstd",
]

[env]
CC = "${ZCHOST}-gcc"
