# Config file for a project
# Placed in $SRCROOT/zcbe/conf.toml
[package]
name = "gdb"
ver = "16.2"

[deps]
build = [
"bison",
"flex"
]
req=[
"flex",
"pdcurses",
"readline",
"termcap",
"expat",
"mpfr",
"xz",
]

[env]
CC = "${ZCHOST}-gcc"
