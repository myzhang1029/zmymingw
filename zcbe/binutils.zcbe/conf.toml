# Config file for a project
# Placed in $SRCROOT/zcbe/conf.toml
[package]
name = "binutils"
ver = "2.44"

[deps]
req = [
"gmp",
"mpfr",
"mpc",
"isl",
"gc",
"libintl",
"zstd",
]

[env]
CC = "${ZCHOST}-gcc"
