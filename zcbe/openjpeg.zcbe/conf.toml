# Config file for a project
# Placed in $SRCROOT/zcbe/conf.toml
[package]
name = "openjpeg"
ver = "2.5.4"

[deps] # Dependencies
build = [
"sphinx-doc",
"cmake",
]
req = [
"lcms2",
"libpng",
"tiff",
"zlib",
]
