# Config file for a project
# Placed in $SRCROOT/zcbe/conf.toml
[package]
name = "fontconfig"
ver = "2.17.1"

[deps]
build = [
"pkg-config",
"gperf",
]
req = [
"expat",
"freetype",
"libpng",
"libintl",
]
