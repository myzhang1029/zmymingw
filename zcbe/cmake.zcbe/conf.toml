# Config file for a project
# Placed in $SRCROOT/zcbe/conf.toml
[package]
name = "cmake"
ver = "4.2.1"

[deps] # Dependencies
build = [
"sphinx-doc",
"cmake",
]
req = [
"zlib",
"nghttp2",
"xz",
"bzip2",
]
