# Config file for a project
# Placed in $SRCROOT/zcbe/conf.toml
[package]
name = "curl"
ver = "8.16.0"

[deps]
req = [
"libpsl",
"libssh2",
"libressl",
"nghttp2",
"zlib",
]

[env]
CC = "${ZCHOST}-gcc"
LDFLAGS = "-L${ZCPREF}/lib"
CFLAGS = "-isystem ${ZCPREF}/include"
